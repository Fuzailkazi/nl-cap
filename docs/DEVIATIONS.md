# DEVIATIONS.md — logged overrides of the CLAUDE.md contract

> The contract (`CLAUDE.md`) is binding. Only explicit user instructions in
> chat can override it, and every override is recorded here with a one-line
> reason. Newest entries at the bottom.

| # | Date | Deviation | Reason | Authorized by |
|---|------|-----------|--------|---------------|
| 1 | 2026-06-11 | Stack is **Next.js 16 / React 19 / Tailwind v4**, not the spec's Next 14. | Deps were bumped to latest on request during scaffold; the existing M0 scaffold already targets 16 and downgrading would break it for no benefit. | User |
| 2 | 2026-06-11 | The **subagent boundaries** in CLAUDE.md are kept as a documentation/discipline map only — they are **not** wired as formal `.claude/agents/` definitions. | User scoped subagents as "just a build tool," not a capstone deliverable. The main thread integrates and may dispatch ephemeral subagents within these boundaries. | User |
| 3 | 2026-06-11 | The MCP notes side-effect table is **`mcp_notes_docs`** (as in `0001_init.sql`), not `shared_doc_entries` as named in the contract's ownership map. | The migration is already applied and is the source of truth; the contract text used an older name. | Schema of record |
| 4 | 2026-06-11 | **LLM generation moved from Anthropic (`claude-sonnet-4-6`) to OpenAI (`gpt-4.1`, env `OPENAI_GEN_MODEL`).** OpenAI is now the **only** LLM vendor — it powers both generation and embeddings. The contract's "one non-Anthropic vendor" framing no longer applies. | User instruction: use one OpenAI key for both generation and embeddings. Revert by pointing `requireGeneration()` back at `ANTHROPIC_API_KEY`. | User |
| 5 | 2026-06-11 | **Advice-refusal AMFI link changed from `https://www.amfiindia.com/investor-corner` to `https://www.amfiindia.com/`.** The verbatim string is updated in lockstep across CLAUDE.md, `evals/datasets/adversarial.json`, and `evals/adversarial.ts`. | The contracted `/investor-corner` path now returns 404; a dead link fails the rule's purpose ("point users to unbiased investor education"). The root is the live official AMFI home. | Recommended by agent (best approach to satisfy the requirement); revert if you prefer the literal original string. |
| 6 | 2026-06-11 | **`data/source-manifest.json` re-seeded (v1→v2): 21 dead URLs removed, replaced with 19 verified HTTP-200 official URLs.** All scheme-specific HDFC `dam/abc/*.pdf` paths were placeholders that 404'd; real paths are `/explore/mutual-funds/hdfc-<slug>/direct` (slug `hdfc-mid-cap-fund`, not `-opportunities-`). golden.json citations repointed to verified URLs. | The contract forbids embedding unverified sources; the seed manifest could not produce a real corpus. Recovered the live official URLs instead of fabricating content. | Recommended by agent |
